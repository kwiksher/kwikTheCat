local M = {}

function M:init(UI)
  local  = UI.sceneGroup[""]
  local  == UI.sceneGroup[""]
  self.name = "",
  self.class ="joint",
  self.properties = {
    bodyA = "",
    bodyB = "",
    type = "", --pistoin, distance, pulle, + defaultSet
  }
  }
end

return require("components.kwik.page_physicsJoint").set(M)