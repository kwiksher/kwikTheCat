local props = {
  name       = "groupC",
  members   = {
    "background1",
    "boo"
  },
  properties = {
    alpha = NIL,
    xScale = NIL,
    yScale = NIL,
    rotation = NIL,
    isLuaTable = true
  }
}

return props -- require("components.kwik.page_group").set(props)
